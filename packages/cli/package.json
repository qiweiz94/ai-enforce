{
  "name": "ai-enforce",
  "version": "0.1.0",
  "description": "Pre-commit hooks for AI coding assistants. Enforce policies across Cline, Claude Code, Cursor, and more.",
  "type": "module",
  "bin": {
    "ai-enforce": "./bin/ai-enforce.js"
  },
  "files": ["dist", "bin", "README.md"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "postinstall": "node ./dist/postinstall.js"
  },
  "dependencies": {
    "@ai-enforce/core": "*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/nanoclaw/ai-enforce.git"
  }
}
